{
  "name": "sprena-functions",
  "version": "1.0.0",
  "description": "Cloud Functions do Sprena — exclusão de conta (LGPD art. 18, VI)",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src",
    "test": "node --test",
    "test:emulator": "firebase --config ../firebase.json emulators:exec --only auth,firestore,functions --project demo-sprena \"npm test\""
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "firebase": "^11.1.0",
    "firebase-tools": "^15.26.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  }
}
